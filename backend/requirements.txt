# Terra backend runtime dependencies (Python 3.11+)
# Core API
fastapi>=0.110
uvicorn>=0.29
mangum>=0.17          # ASGI -> AWS Lambda adapter
pydantic>=2.6
pydantic-settings>=2.2
httpx>=0.27
python-jose[cryptography]>=3.3   # verifies Cognito JWTs against the pool's JWKS; pure crypto, no AWS SDK/creds needed

# Geospatial / EO processing
rasterio>=1.3         # windowed COG reads (bundles GDAL)
pystac-client>=0.7    # STAC discovery (Earth Search)
shapely>=2.0          # geometry validation
numpy>=1.24

# AWS SDK: intentionally NOT listed here. Cloud adapters (DynamoDB job store,
# SQS worker, Bedrock) import boto3 lazily, only when actually constructed
# against a real AWS resource, so the default dev/test environment never
# needs it (§61 — offline by design). The Lambda deploy container doesn't
# need it declared either: the `public.ecr.aws/lambda/python` base image
# ships boto3/botocore already. If you want to exercise the real AWS SDK
# locally (e.g. against a local DynamoDB/SQS endpoint), install it yourself:
# pip install boto3
